[{@} - без остановки скрипта от ошибок]table {имя таблицы}/{поля сортировки}[:{имя таблицы, с которой конвертируем}][?{условие выборки}]
{имя столбца}[:{выбираемое значение, доступен синтаксис MySQL}]
{имя столбца2}|function:{имя функции, суффикс к методам "get_"}({список аргументов из выбираемых 
                                        столбцов или $row, где возвращается весь массив ВЫБОРКИ})
{пустая строка}
etc... Лучший пример в install/database/cyberhype.conv +
install/database/cyberhype.php. Перед конвертацией данная таблица в CTRev
очищается.